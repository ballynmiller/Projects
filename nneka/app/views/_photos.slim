- unless photos.empty?
	div id="images"
		- for photo in photos do
			img src="#{photo}" class="item" width="300"

javascript:
	$(window).load(function(){
		$('#images').masonry({
			itemSelector: '.item',
			columnWidth: 152
		});
	}); 